user-interface bugs:
  * shortcut Ctrl-P visible in Menu (pending)
  * open all subtrees

code improvements:
  * Add module descriptions (haddock)
  * GTK dialogs with response 
  * restructure GUI to multiple modules

feature requests:
  * srcloc :: Maybe (Either ParsedFile ParsedError-> (Int,Int))
  * load additional parsers at runtime (user-triggered, open file)
  * new string field in Parser for gtk-syntaxhighlighter
  * show subtree when subtree is collapsed
  * generate and load bnfc-parser at runtime
  * generate pdf/png graphs from trees or terms
  * define/parse/toTree generically (using generic read)
